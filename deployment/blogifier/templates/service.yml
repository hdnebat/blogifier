kind: Service
apiVersion: v1
metadata:
  name: blog-{{.Release.Name}}
  namespace: {{.Values.namepsace}}
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    io.godwit.service:  blog-{{.Release.Name}}
  type: ClusterIP
---